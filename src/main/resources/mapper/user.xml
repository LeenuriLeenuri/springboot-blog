<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cos.blog.repository.UserRepository">

	<select id="findAll" resultType="com.cos.blog.model.user.User">
		select * from user order by id
		desc
	</select>

	<select id="save" resultType="com.cos.blog.model.user.User">
		insert into user(username,
		password, eamil, createDate)
		value(#{username}, #{password}, #{email},
		now())
	</select>

	<select id="findByUsername" resultType="int">
		select count(*) from user where username=#{username}
	</select>


</mapper>